### tmux keybindings
**Press `<Prefix>` key combination (`Ctrl+b`, by default) before each shortcut**
|||split-window -h|
|-|split-window -v|
|h|select-pane -L|
|k|select-pane -U|
|j|select-pane -D|
|l|select-pane -R|
